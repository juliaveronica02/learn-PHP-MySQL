<?php
    // standard output.
    // echo: print.
    echo "Julia &nbsp"; 
    // print_r
    print_r ("Julia Veronica &nbsp");
    // var_dump (hasil output string/boolean (true and false)).
    var_dump("Julia");

    // $ variable.
    $first_name = "Julia";
    $last_name = "Veronica";
    echo $first_name. $last_name;
    echo $first_name. " " .$last_name;

    // assignment.
    // =, +=, -=, *=, /=, %=, .= .
    $x = 1;
    $x -= 5;
    echo $x;
    $name = "Julia";
    $name .= " ";
    $name .= "Veronica";
    echo $name;
?>